name: Build and Submit Android

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and Submit Android
    runs-on: ubuntu-latest

    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}
      ANDROID_SERVICE_ACCOUNT_JSON: ${{ secrets.ANDROID_SERVICE_ACCOUNT_JSON }}

    steps:
      # 1️⃣ Checkout
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Cache Yarn dependencies
      - name: Cache Yarn dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/yarn
            node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: yarn install

      # 5️⃣ Install EAS CLI
      - name: Install EAS CLI
        run: yarn global add eas-cli

      # 6️⃣ Configure Google Play credentials
      - name: Configure Google Play credentials
        run: |
          mkdir -p ./credentials
          printf "%s" "${ANDROID_SERVICE_ACCOUNT_JSON}" > ./credentials/service-account.json
          echo "Service account JSON copied successfully."

      # 7️⃣ Validate service account JSON
      - name: Validate Google Service Account JSON
        run: |
          jq '.' ./credentials/service-account.json
          jq -e '.type, .project_id, .private_key_id, .private_key, .client_email' ./credentials/service-account.json
          echo "Service account JSON structure is valid."

      # 8️⃣ Determine EAS profile
      - name: Determine EAS profile
        id: profile
        run: echo "profile=staging" >> $GITHUB_OUTPUT

      # 9️⃣ Try to restore existing artifact
      - name: Restore previous build artifact
        id: restore-artifact
        uses: actions/download-artifact@v4
        with:
          name: app-release
          path: ./builds
        continue-on-error: true

      # 🔟 Check if artifact exists
      - name: Check if AAB exists
        id: check-artifact
        run: |
          if [ -f ./builds/app-release.aab ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      # 1️⃣1️⃣ Build Android only if artifact does not exist
      - name: Build Android (Local)
        if: steps.check-artifact.outputs.exists == 'false'
        run: |
          mkdir -p ./builds
          export NODE_OPTIONS="--max-old-space-size=4096"
          eas build --platform android \
            --profile ${{ steps.profile.outputs.profile }} \
            --local --output ./builds/app-release.aab \
            --non-interactive

      # 1️⃣2️⃣ Upload AAB artifact
      - name: Upload AAB artifact
        if: steps.check-artifact.outputs.exists == 'false'
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: ./builds/app-release.aab

      # 1️⃣3️⃣ Submit to Google Play
      - name: Submit to Google Play
        run: |
          eas submit --platform android \
            --path ./builds/app-release.aab \
            --profile staging \
            --non-interactive
